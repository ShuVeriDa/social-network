import {ActionsTypes, SidebarType} from "./store";

let inititalState: SidebarType = {}

export const sidebarReducer = (state: SidebarType = inititalState, action: ActionsTypes) => {

   return state
}